# Problem Statement

The embedding model must be suitable for code, not just natural language. It should capture code structure, architecture, semantics, logic, and syntax well enough to support accurate retrieval across the target codebase.

If a model works well only for NLP, the research scope may need to be narrowed to languages and projects where code is closer to natural language, such as Python.